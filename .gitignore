# In your project root
ni .gitignore -Value @"
# Python
.venv/
__pycache__/
*.pyc
mood.db

# Node / Expo / React Native
node_modules/
.expo/
dist/
build/
.android/
.ios/
ios/
android/
.next/

# Models / datasets / checkpoints
model/*.pt
*.onnx
*.bin
*.ckpt

# OS
.DS_Store
"@

model.zip
.venv.zip
model/

# Stop tracking anything already committed
git rm -r --cached .venv node_modules ios android model/*.pt mood.db .expo dist build .next
git add .
git commit -m "Prune large/unwanted files; add .gitignore"
